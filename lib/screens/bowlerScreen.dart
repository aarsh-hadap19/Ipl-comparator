import 'package:flutter/material.dart';

class bolwerScreen extends StatefulWidget {
  const bolwerScreen({super.key});

  @override
  State<bolwerScreen> createState() => _bolwerScreenState();
}

class _bolwerScreenState extends State<bolwerScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Bolwer"),
    );
  }
}
